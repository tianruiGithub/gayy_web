.login-box
  .login-logo{:style => "color: white;"} 国安药业管理后台
  .login-box-body
    %p.login-box-msg 请输入您的帐号密码
    = simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      = f.input :email, :required => false, :autofocus => true, :class => "form-control", :placeHolder => "帐号"
      %span.glyphicon.glyphicon-envelope.form-control-feedback
      = f.input :password, :required => false, :autofocus => true, :class => "form-control", :placeHolder => "密码"
      %span.glyphicon.glyphicon-lock.form-control-feedback
      -#= f.input :remember_me, as: :boolean if devise_mapping.rememberable?
      = f.button :submit, "登录", :class => "btn btn-primary btn-block btn-flat"

